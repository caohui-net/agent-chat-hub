# 快速启动指南 - 文件上传功能

## 🚀 立即开始

```bash
# 方式1: 使用启动脚本（推荐）
./start.sh

# 方式2: 手动启动
source venv/bin/activate  # 先激活虚拟环境
python3 main.py
```

---

## 📤 文件上传 - 3种方式

### 方式1: 文件浏览器（推荐）
1. 点击右侧 `📤 浏览上传` 按钮
2. 像使用文件管理器一样导航
3. 双击文件或按Enter选择
4. 自动返回，文件出现在列表

### 方式2: 路径输入
1. 点击 `📤 浏览上传`
2. 在底部输入框粘贴路径
3. 按Enter跳转/选择

### 方式3: 快捷导航
1. 打开文件浏览器
2. 用↑/↓键选择文件
3. 按Enter进入目录或选择文件
4. 按Backspace返回上一级

---

## 📝 查看文件内容

1. 在右侧文件列表选择文件（↑/↓键）
2. 点击 `📝 查看内容`
3. 聊天区显示文件内容（前50行）

### 返回聊天
- **方式1**: 点击 `↩️ 返回聊天` 按钮
- **方式2**: 按 `Ctrl+B` 快捷键

---

## 🗑️ 移除文件

1. 选择要移除的文件
2. 点击 `🗑️ 移除选中`
3. 文件从列表消失（原文件不删除）

---

## ⌨️ 快捷键

| 快捷键 | 功能 | 使用场景 |
|--------|------|----------|
| `Ctrl+C` | 退出应用 | 任何时候 |
| `Ctrl+N` | 新会话 | 任何时候 |
| `Ctrl+B` | 返回聊天 | 查看文件后 |
| `Esc` | 取消 | 文件浏览器 |
| `Enter` | 选择 | 文件浏览器 |
| `Backspace` | 上一级 | 文件浏览器 |
| `↑/↓` | 导航 | 列表选择 |

---

## 📋 常见操作

### 上传文本文件
```bash
# 创建测试文件
echo "Hello World" > /tmp/test.txt

# 在应用中:
1. 点击 📤 浏览上传
2. 导航到 /tmp
3. 选择 test.txt
```

### 批量上传多个文件
```bash
# 重复上传流程即可
# 每次选择一个文件
# 文件列表会实时更新
```

### 查看文件并返回
```bash
# 在应用中:
1. 选择文件
2. 点击 📝 查看内容
3. 按 Ctrl+B 返回
```

---

## ⚠️ 注意事项

1. **文件大小限制**: 最大10MB
2. **文件预览**: 仅显示前50行
3. **大文件**: >1MB只显示路径
4. **文件移除**: 不删除原文件，只从列表移除

---

## 🐛 故障排除

### 无法选择文件
- 检查文件是否存在
- 检查文件大小（<10MB）
- 检查文件权限

### 文件浏览器无响应
- 按Esc退出重试
- 检查目录权限

### 查看文件后看不到内容
- 检查文件编码（应为UTF-8）
- 大文件（>1MB）只显示路径

### 无法返回聊天
- 按 Ctrl+B 快捷键
- 点击 ↩️ 返回聊天按钮
- 如果没有备份，会显示当前会话历史

---

## 📚 更多文档

- `COMPLETION_SUMMARY.md` - 完整功能总结
- `FILE_UPLOAD_COMPLETE.md` - 详细使用说明
- `UI_IMPROVEMENTS.md` - UI改进详情
- `TESTING_CHECKLIST.md` - 测试清单

---

## 💡 小贴士

1. **路径快捷方式**: 
   - `~` 代表用户主目录
   - `./` 代表当前目录
   - `/tmp` 是临时文件好去处

2. **快速导航**: 
   - 在路径输入框直接跳转
   - 比逐级点击快得多

3. **文件管理**: 
   - 先上传多个文件
   - 再逐个查看内容
   - 用完移除保持整洁

4. **快捷键效率**: 
   - `Ctrl+B` 比点按钮快
   - `Enter` 比双击快
   - `Backspace` 比点上一级快

---

**祝使用愉快！** 🎉
