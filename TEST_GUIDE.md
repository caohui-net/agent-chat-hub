# TUI复制粘贴功能 - 用户测试指南

## 测试前准备

1. 确保所有更改已应用：
   ```bash
   git status
   # 应该看到 worktree-session-init 分支
   ```

2. 启动应用：
   ```bash
   python3 main.py
   ```

---

## 测试项目

### ✅ 测试1：应用内复制功能

**步骤：**
1. 应用启动后，发送几条消息与Agent对话
2. 等待聊天显示区有内容
3. 按 `Ctrl+Shift+C` 快捷键

**预期结果：**
- 显示通知："已复制到剪贴板（OSC 52）"或类似消息
- 或显示："已复制到剪贴板（xclip/wl-copy/pbcopy）"
- 如果失败，显示降级提示

**验证复制成功：**
- 打开文本编辑器
- 按 `Ctrl+V`（Linux/Windows）或 `Cmd+V`（macOS）
- 应该粘贴出聊天内容

---

### ✅ 测试2：退出功能

**步骤：**
1. 在应用运行时
2. 按 `Ctrl+Q` 快捷键

**预期结果：**
- 应用正常退出
- 不会显示错误信息

**验证：**
- ❌ 按 `Ctrl+C` 不应该退出应用（已改为Ctrl+Q）
- ✅ 按 `Ctrl+Q` 应该立即退出

---

### ✅ 测试3：终端原生复制

**步骤：**
1. 应用启动后，聊天显示区有内容
2. 按住 `Shift` 键
3. 用鼠标拖拽选择文本
4. 使用终端的复制快捷键：
   - Linux/Windows Terminal: `Ctrl+Shift+C`
   - macOS Terminal: `Cmd+C`
   - 或右键菜单选择"复制"

**预期结果：**
- 文本被高亮选中
- 复制到系统剪贴板

**验证：**
- 打开文本编辑器
- 粘贴内容
- 应该看到选中的文本

---

### ✅ 测试4：快捷键提示

**步骤：**
1. 应用运行时，查看底部Footer

**预期结果：**
应该看到快捷键提示：
- `Ctrl+Q` - 退出
- `Ctrl+Shift+C` - 复制消息
- `Ctrl+T` - 切换Agent
- `Ctrl+R` - 刷新Agent列表
- `Ctrl+G` - 配置管理
- `Ctrl+P` - 插件管理

---

### ✅ 测试5：复制失败降级

**步骤：**
1. （可选）在无剪贴板工具的环境中测试
2. 按 `Ctrl+Shift+C`

**预期结果：**
如果所有复制策略失败，应该显示降级提示：
```
剪贴板不可用。请使用终端原生复制：
1. 按住 Shift 键
2. 用鼠标拖拽选择文本
3. 使用终端的复制快捷键
   (macOS: Cmd+C / Linux/Windows: Ctrl+Shift+C)
```

---

## 常见问题

### Q: Ctrl+Shift+C复制后，粘贴没有内容？

**可能原因：**
1. 终端不支持OSC 52
2. 剪贴板工具未安装

**解决方法：**
- 使用终端原生复制（Shift+拖拽）
- 安装剪贴板工具：
  - Linux: `sudo apt install xclip` 或 `sudo apt install wl-clipboard`
  - macOS: 内置pbcopy，无需安装

---

### Q: Ctrl+C不能退出应用了？

**这是正确的！**
- 退出快捷键已改为 `Ctrl+Q`
- 这是为了解决Ctrl+C的语义冲突

---

### Q: 如何复制特定的一条消息？

**当前版本：**
- `Ctrl+Shift+C` 复制整个聊天显示区
- 要复制特定内容，使用终端原生选择（Shift+拖拽）

**未来版本：**
- 可能会添加"复制当前消息"功能

---

## 测试完成清单

- [ ] 测试1：应用内复制功能
- [ ] 测试2：退出功能（Ctrl+Q）
- [ ] 测试3：终端原生复制
- [ ] 测试4：快捷键提示显示正确
- [ ] 测试5：复制失败降级提示

---

## 报告问题

如果发现任何问题，请记录：
1. 操作步骤
2. 预期结果
3. 实际结果
4. 错误信息（如果有）
5. 终端类型和操作系统

---

**文档版本：** v1.0  
**创建日期：** 2026-07-26  
**对应提交：** 7ffebea
