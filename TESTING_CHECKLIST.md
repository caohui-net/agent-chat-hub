# 测试验证清单 - 文件上传与UI改进

## 快速测试指南

### 启动应用
```bash
# 推荐: 使用启动脚本
./start.sh

# 或者手动启动（需先激活虚拟环境）
source venv/bin/activate
python3 main.py
```

---

## Round 1: 文件浏览器功能

### ✅ 测试1: 文件系统列表显示
1. 点击右侧 `📤 浏览上传` 按钮
2. **预期**: 打开文件浏览器，显示目录和文件列表
3. **检查**: 
   - 显示图标（📁目录 📄文件）
   - 显示文件大小、类型、修改时间
   - 目录排在前面

### ✅ 测试2: 目录导航
1. 在文件浏览器中选择一个目录
2. 双击或按Enter键
3. **预期**: 进入该目录，显示子目录和文件
4. 按Backspace或点击 `⬆️ 上一级`
5. **预期**: 返回父目录

### ✅ 测试3: 路径复制粘贴
1. 在文件浏览器底部输入框中粘贴路径：`~/Documents/test.txt`
2. 按Enter键
3. **预期**: 
   - 如果是目录：跳转到该目录
   - 如果是文件：选择该文件并返回

### ✅ 测试4: 取消返回
1. 在文件浏览器中按Esc键
2. **预期**: 关闭浏览器，返回主界面
3. 或点击 `✗ 取消` 按钮
4. **预期**: 同样返回主界面

### ✅ 测试5: 选择文件
1. 选择一个文件（小于10MB）
2. 双击或按Enter键
3. **预期**: 文件添加到右侧列表，自动返回主界面
4. **检查**: 聊天区显示 `✅ 文件已上传: xxx`

### ✅ 测试6: 文件列表显示
1. 查看右侧文件面板
2. **预期**: 
   - 显示已上传的文件名、大小、类型
   - 底部显示 `共 X 个文件`
   - 文件带有 📄 图标

---

## Round 2: UI改进功能

### ✅ 测试7: 查看文件内容
1. 在右侧文件列表选择一个文件（用↑/↓键）
2. 点击 `📝 查看内容` 按钮
3. **预期**: 
   - 聊天区显示文件内容（前50行）
   - 底部显示提示：`💡 提示: 按Ctrl+B返回聊天历史`

### ✅ 测试8: 返回聊天 - 按钮方式
1. 查看文件后，点击 `↩️ 返回聊天` 按钮
2. **预期**: 聊天区恢复原来的对话内容

### ✅ 测试9: 返回聊天 - 快捷键方式
1. 查看文件后，按 `Ctrl+B` 键
2. **预期**: 聊天区恢复原来的对话内容

### ✅ 测试10: 移除文件
1. 在文件列表选择一个文件
2. 点击 `🗑️ 移除选中` 按钮
3. **预期**: 
   - 文件从列表消失
   - 聊天区显示：`🗑️ 已从列表移除: xxx (文件本身未删除)`

### ✅ 测试11: 面板宽度验证
1. 观察界面布局
2. **检查**: 
   - Agent面板在左侧（约30列宽）
   - 聊天区在中间（自适应宽度）
   - 文件面板在右侧（约35列宽）
3. 调整终端窗口大小
4. **预期**: 各面板按比例缩放

---

## 集成测试场景

### 场景1: 完整文件上传流程
1. 点击 `📤 浏览上传`
2. 导航到 `/tmp` 目录
3. 创建测试文件：在底部输入 `/tmp/test.txt` 并Enter
4. 如果不存在，先创建：`echo "Hello World" > /tmp/test.txt`
5. 选择 `test.txt` 文件
6. 验证文件出现在右侧列表
7. 点击 `📝 查看内容`
8. 验证显示 "Hello World"
9. 按 `Ctrl+B` 返回
10. 点击 `🗑️ 移除选中`
11. 验证文件从列表消失

### 场景2: 多文件管理
1. 上传3个不同的文件
2. 验证文件列表显示 `共 3 个文件`
3. 依次查看每个文件内容
4. 每次查看后使用不同方式返回：
   - 第1次：点击按钮
   - 第2次：按Ctrl+B
   - 第3次：点击按钮
5. 依次移除所有文件
6. 验证最终显示 `📭 暂无文件` 和 `共 0 个文件`

### 场景3: 边界情况
1. 尝试选择不存在的文件
2. 尝试查看已删除的文件
3. 尝试上传大于10MB的文件
4. 在没有文件时点击"查看内容"
5. 在没有备份时按Ctrl+B

---

## 快捷键测试

| 快捷键 | 功能 | 测试 |
|--------|------|------|
| `Ctrl+C` | 退出应用 | [ ] |
| `Ctrl+N` | 新会话 | [ ] |
| `Ctrl+B` | 返回聊天 | [ ] |
| `Esc` | 取消/关闭浏览器 | [ ] |
| `Enter` | 选择文件/进入目录 | [ ] |
| `Backspace` | 返回上一级目录 | [ ] |
| `↑/↓` | 导航列表 | [ ] |

---

## 性能测试

### 大文件处理
- [ ] 上传接近10MB的文件
- [ ] 验证大小限制检查
- [ ] 查看大文件（>1MB）显示路径而非内容

### 长文件处理
- [ ] 上传超过50行的文件
- [ ] 验证只显示前50行
- [ ] 验证提示 "共 X 行，仅显示前50行"

### 目录深度
- [ ] 导航到深层目录（5级以上）
- [ ] 验证路径显示正确
- [ ] 验证返回上一级正常

---

## 回归测试

确保原有功能未受影响：

- [ ] Agent面板显示正常
- [ ] 聊天功能正常
- [ ] 消息历史保存正常
- [ ] Token统计正常
- [ ] 状态栏更新正常
- [ ] 配置管理正常
- [ ] 插件管理正常

---

## 已知限制

1. **文件大小**: 最大10MB
2. **文件预览**: 仅显示前50行
3. **面板宽度**: CSS固定范围（需重启修改）
4. **拖拽上传**: 不支持（终端限制）

---

## 测试报告模板

```
测试日期: ____/____/____
测试人员: ______________
环境: Ubuntu ____ / Python ____

Round 1 测试结果: ___/6 通过
Round 2 测试结果: ___/5 通过
集成测试结果: ___/3 通过
快捷键测试: ___/7 通过
性能测试: ___/3 通过
回归测试: ___/7 通过

总计: ___/31 通过

备注:
_______________________________________________
_______________________________________________
```

---

**文档版本**: v1.0  
**创建日期**: 2026-09-08  
**关联文档**: 
- FILE_UPLOAD_COMPLETE.md
- UI_IMPROVEMENTS.md
- FILE_UPLOAD_IMPROVEMENTS.md
